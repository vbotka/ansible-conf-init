---
ai_repo: git://10.10.99.201

ai_db_host:
  foo:
    repo_host: "{{ ai_repo }}"
    repo: ansible-conf-test
    repo_dest: /root
    repo_playbook: pb-test.yml
  bar:
    repo_host: "{{ ai_repo }}"
    repo: ansible-conf-test
    repo_dest: /root
    repo_playbook: pb-test.yml

ai_db_class_options:
  at: ''
  async: true

ai_db_class:
  log-server:
    repo_host: "{{ ai_repo }}"
    repo: ansible-conf-syslogng-server
    repo_dest: /root
    repo_playbook: pb-logserv.yml
  log-client:
    repo_host: "{{ ai_repo }}"
    repo: ansible-conf-syslogng-client
    repo_dest: /root
    repo_playbook: pb-logclient.yml
  test:
    repo_host: "{{ ai_repo }}"
    repo: ansible-conf-test
    repo_dest: /root
    repo_playbook: pb-test.yml
